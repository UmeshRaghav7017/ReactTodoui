name: Frontend application pipeline

trigger: none

pool: KarunaPool


stages:
    - stage: checkoutstage
      jobs:
          - job: checkoutjob
            steps:
                - checkout: self

    - stage: 
      jobs:
          - job: npminstalljob
            steps:
                - task: Npm@1
                  inputs:
                    command: 'install'
                    workingDir: '$(System.DefaultWorkingDirectory)'

                - task: Npm@1
                  inputs:
                    command: 'custom'
                    workingDir: 'run build'
                    customCommand: 'C:\Users\HP\Downloads\vsts-agent-win-x64-4.264.2\_work\3\s'
